<% if @pub_sub.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@pub_sub.errors.count, "error") %> prohibited this pub_sub from being saved:</h2>

    <ul>
    <% @pub_sub.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>